/**
 * Configuração pública exclusiva do backend Gemini.
 * O Deployment ID identifica a API autenticada e não é uma credencial.
 * Nunca coloque a chave Gemini neste arquivo.
 */
window.LANCHES_AI_CONFIG = Object.freeze({
  appsScriptDeploymentId: ""
});
